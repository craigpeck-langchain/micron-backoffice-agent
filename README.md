# Micron Back-Office Agent Demo

Inbound email -> shipping order / purchase order / invoice / remittance advice automation, built with LangGraph + deepagents, instrumented for a LangSmith Engine demo.

Run the dev server:

```
uv sync
uv run langgraph dev
```
